-r requirements.txt

# EasyOCR runs with gpu=False in app.analysis.ocr, so use PyTorch CPU wheels.
# Leaving torch unresolved lets pip install the default CUDA wheel stack.
--extra-index-url https://download.pytorch.org/whl/cpu
torch==2.12.0+cpu
torchvision==0.27.0+cpu
easyocr==1.7.2
